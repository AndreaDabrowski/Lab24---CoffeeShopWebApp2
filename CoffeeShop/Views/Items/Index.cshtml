@model IEnumerable<CoffeeShop.Models.Item>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.i_description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.i_quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.i_price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.i_description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.i_quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.i_price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.i_name }) |
            @Html.ActionLink("Details", "Details", new { id=item.i_name }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.i_name })
        </td>
    </tr>
}

</table>
